{
  "name": "defi-security-suite",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "contracts",
    "extension",
    "webapp"
  ],
  "scripts": {
    "install:all": "npm install && npm run install:backend && npm run install:contracts && npm run install:extension && npm run install:webapp",
    "install:backend": "cd backend && npm install",
    "install:contracts": "cd contracts && npm install",
    "install:extension": "cd extension && npm install",
    "install:webapp": "cd webapp && npm install"
  }
}
